# packages/contracts

Shared TypeScript API contract placeholders for Play The Position.

Current scope:
- Export shared response and error types only.
- No runtime validation, OpenAPI generation, backend integration, or frontend integration yet.

Commands:
- `pnpm --filter contracts build`
